-- Прибуток, порахований САМИМ касиром (необовʼязкове поле). Не бере участі в
-- розрахунках — збираємо, щоб порівняти логіку касирів із моделями системи.
ALTER TABLE "Operation" ADD COLUMN "cashierProfit" DECIMAL(15,2);
